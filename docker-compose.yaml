services:
  streamlit:
    build: .
    container_name: food-additives-dashboard
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./food_additives.db:/app/food_additives.db
      - ./additive_databases:/app/additive_databases:ro
